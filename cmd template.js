const Discord = require("discord.js");

module.exports = {
    name: "_",
    aliases: ["_", "_"],
    category: "_",
    description: "_",
    usage: "_",
    permission: "_",
    run: async (client, msg, arg) => {
        
    }
}
