

module.exports = {
    name: "queue",
    category: "Music",
    description: "Returns song queue list",
    usage: "queue\n**e.g.**\n\`queue\`\n> Check which songs are in the queue",
    run: async (client, msg, arg) => {

    }
}
